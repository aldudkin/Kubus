node_modules/
dist/
*.tsbuildinfo

# built helm rendering engine (node helm-engine/build.mjs, requires Go)
server/assets/helm-engine.wasm.gz
helm-engine/helm-engine.wasm
electron/build/
electron/release/
.env
.DS_Store

# Zensical docs build output & cache
site/
.cache/

# local agent and lock metadata
.agents/
.claude/skills
skills-lock.json

# promo video project (HyperFrames, local-only)
/promo/
